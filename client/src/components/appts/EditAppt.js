import { useEffect, useState } from "react";
import { useParams } from "react-router-dom";

export const EditAppt = () => {
  //   const {id} = useParams();
  const [appt, setAppt] = useState();

  //   useEffect(()=> {
  //     getApptById().then(setAppt)
  //   },[])

  return <>EditAppt View</>;
};
